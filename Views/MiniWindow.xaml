<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="ShutdownTimer.Views.MiniWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Timer">

    <Grid x:Name="RootGrid" Background="#E6101020" Padding="16,8">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <TextBlock x:Name="TimerText" Grid.Column="0"
                   Text="--:--:--"
                   FontSize="22" FontWeight="SemiLight"
                   FontFamily="Segoe UI Variable Display"
                   Foreground="#00D4FF"
                   VerticalAlignment="Center" />

        <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="6" VerticalAlignment="Center">
            <Button x:Name="ExpandBtn" Content="&#xE740;" FontFamily="Segoe MDL2 Assets" FontSize="10"
                    Click="ExpandBtn_Click"
                    Background="Transparent" Foreground="#8888AA"
                    BorderThickness="0" Padding="4" CornerRadius="4" />
            <Button x:Name="CloseBtn" Content="&#xE711;" FontFamily="Segoe MDL2 Assets" FontSize="10"
                    Click="CloseBtn_Click"
                    Background="Transparent" Foreground="#8888AA"
                    BorderThickness="0" Padding="4" CornerRadius="4" />
        </StackPanel>
    </Grid>
</Window>
